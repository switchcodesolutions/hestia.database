CREATE TABLE [comms].[ShowingRequestStatus]
(
    [Id]    INT             NOT NULL,
    [Name]  NVARCHAR(100)   NOT NULL,

    CONSTRAINT PK_comms_ShowingRequestStatus PRIMARY KEY CLUSTERED ([Id])
);
